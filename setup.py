from setuptools import find_packages, setup
import sys


setup(
    name='eworm',
    version='0.0.1',
    description="setup for package reference",
    packages=find_packages(),
    )
print(sys.path)

